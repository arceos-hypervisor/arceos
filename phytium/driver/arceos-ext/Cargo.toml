[package]
edition = "2024"
name = "arceos-ext"
version = "0.1.0"

[dependencies]
axstd = {workspace = true, features = [
  "alloc",
  "paging",
]}

dma-api = "0.4"
mbarrier = "0.1"

[target.'cfg(target_arch = "aarch64")'.dependencies]
aarch64-cpu-ext = "0.1"
